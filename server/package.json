{
  "name": "ui-bff",
  "description": "Backend for frontend (BFF) for the UI.",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "^22.0.0",
    "npm": "^11.0.0"
  },
  "main": "app.js",
  "scripts": {
    "start": "fastify start -l info -p 3001 app.js",
    "dev": "fastify start -w -l info -p 3001 -P app.js"
  },
  "dependencies": {
    "@fastify/autoload": "^6.3.0",
    "@fastify/cookie": "^11.0.2",
    "@fastify/env": "^5.0.2",
    "@fastify/http-proxy": "^11.1.2",
    "@fastify/secure-session": "^8.2.0",
    "@fastify/sensible": "^6.0.3",
    "fastify": "^5.3.3",
    "fastify-plugin": "^5.0.1"
  }
}
