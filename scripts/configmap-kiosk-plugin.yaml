# This file is auto-generated by scripts/build-kiosk-plugin.sh — do not edit by hand.
# Plugin: kiosk-mode v1.0.0
# Source: https://github.tools.sap/openmcp/headlamp-plugin-kiosk.git
apiVersion: v1
kind: ConfigMap
metadata:
  name: kiosk-plugin
  namespace: headlamp
data:
  package.json: |-
    {"name":"kiosk-mode","version":"1.0.0","devDependencies":{"@kinvolk/headlamp-plugin":"^0.13.1","@types/react":"^18.3.28"}}
  main.js: |-
    (function(i,s){typeof exports=="object"&&typeof module<"u"?s(require("@kinvolk/headlamp-plugin/lib")):typeof define=="function"&&define.amd?define(["@kinvolk/headlamp-plugin/lib"],s):(i=typeof globalThis<"u"?globalThis:i||self,s(i.pluginLib))})(this,(function(i){"use strict";const s=new Set(["home","storage","network","gatewayapi"]);i.registerSidebarEntryFilter(e=>s.has(e.name)?null:e),i.registerAppBarAction({id:"kiosk-strip-appbar-actions",processor:()=>[]});function c(){try{const e=window.location.pathname.match(/^\/c\/([^/]+)/),t=e?e[1]:null;if(!t)return;const r=`headlamp-selected-namespace_${t}`,o=localStorage.getItem(r);(o?JSON.parse(o):[]).length===0&&localStorage.setItem(r,JSON.stringify(["default"]))}catch{}}function l(){try{localStorage.setItem("sidebar",JSON.stringify({shrink:!0}))}catch{}const e=()=>{var t;try{const r=window.pluginLib;if(!r)return!1;const o=(t=r["redux/stores/store"])==null?void 0:t.default,n=r["components/Sidebar/sidebarSlice"];return!o||!(n!=null&&n.setWhetherSidebarOpen)?!1:(o.dispatch(n.setWhetherSidebarOpen(!1)),!0)}catch{return!1}};if(!e()){let t=0;const r=setInterval(()=>{t++,(e()||t>=20)&&clearInterval(r)},100)}}function a(){var o;const e="kiosk-mode-styles";(o=document.getElementById(e))==null||o.remove();const t=document.createElement("style");t.id=e,t.innerHTML=`
        /* ── Hide top app bar (logo, search, settings, user icon) ── */
        nav.MuiAppBar-root,
        nav[aria-label="Appbar Tools"] {
          display: none !important;
        }
    
        /* ── Hide specific built-in sidebar entries by aria-label ── */
        /* registerSidebarEntryFilter only works for plugin-registered entries;  */
        /* built-in entries must be hidden via CSS.                               */
        nav a[aria-label="Storage"],
        nav a[aria-label="Network"],
        nav a[aria-label="Gateway (beta)"] {
          display: none !important;
        }
    
        /* ── Hide all alerts (errors, warnings, info banners) everywhere ── */
        /* This covers: cluster connection errors, permission warnings, etc.     */
        [role="alert"],
        .MuiAlert-root,
        .MuiAlert-standardError,
        .MuiAlert-filledError,
        .MuiAlert-outlinedError,
        .MuiAlert-standardInfo,
        .MuiAlert-standardWarning,
        [class*="clusterError"],
        [class*="ClusterGroupError"] {
          display: none !important;
        }
    
        /* ── Hide the cluster-error banner box (wraps "Something went wrong…") ── */
        /* Headlamp renders this as a plain MuiBox directly inside <main>, not   */
        /* as an alert, so [role="alert"] does not catch it. Target by the MUI   */
        /* hash class and by structural selector (direct child of main with only  */
        /* text + a button, no MuiPaper children).                                */
        main > .MuiBox-root.css-1xoun1b,
        main > .MuiBox-root:not(:has(main)):not(:has(.MuiPaper-root)):not(:has(h1)):not(:has(table)) {
          display: none !important;
        }
    
        /* ── Remove top padding left over from the now-hidden AppBar ── */
        .MuiBox-root.css-1uqao6u {
          padding-top: 0 !important;
          flex-direction: row !important;
        }
    
        /* ── Expand main content area to fill the viewport ── */
        main {
          margin-left: 0 !important;
          padding: 16px !important;
          width: 100% !important;
          max-width: 100% !important;
          flex: 1 !important;
        }
    
        /* ── Make the content+sidebar row fill full height ── */
        .MuiBox-root.css-1xd9zsj {
          width: 100% !important;
        }
      `,document.head.appendChild(t),document.querySelectorAll('[role="alert"], .MuiAlert-root').forEach(n=>{n.style.setProperty("display","none","important")});const r=document.querySelector("main");r&&Array.from(r.children).forEach(n=>{const d=n.textContent||"";(d.includes("Something went wrong")||d.includes("Lost connection"))&&n.style.setProperty("display","none","important")})}typeof window<"u"&&(l(),c(),a(),document.readyState==="loading"&&document.addEventListener("DOMContentLoaded",a),setTimeout(a,100),setTimeout(a,500),setTimeout(a,1500),new MutationObserver(a).observe(document.body,{childList:!0,subtree:!0}),window.addEventListener("popstate",l))}));
